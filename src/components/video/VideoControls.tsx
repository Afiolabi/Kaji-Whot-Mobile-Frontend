import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const VideoControls = () => {
  return (
    <View>
      <Text>VideoControls</Text>
    </View>
  )
}

export default VideoControls

const styles = StyleSheet.create({})